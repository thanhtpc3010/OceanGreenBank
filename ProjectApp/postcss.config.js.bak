module.exports = {
  plugins: {
    '@tailwindcss/postcss': {
      // Base path để Tailwind quét nội dung (quan trọng khi build qua esbuild của Angular,
      // nếu không nó sẽ không tìm thấy template Angular -> không sinh utility classes).
      base: './src'
    }
  }
};
